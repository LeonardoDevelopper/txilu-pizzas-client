rrrr	rimport { View, Text, Image, Modal, StyleSheet, TouchableHighlight} from 'react-native'
import { ButtonNavigation } from '../form/button'

const HeaderScreenModal = ({fun}) => {
    function close() {

    }
    return (
        <View style={styles.header}>
            <View style={styles.containerBack}>
            <TouchableHighlight
                    underlayColor={'#f5f5f5f5'}        
                    style={styles.btnNav}
                    onPress={() => {
                alert('HELLO');
                        fun()
                    }}
                >
                <Image style={styles.ico} source={require('../../../assets/left.png')} />
            </TouchableHighlight>
                <Text style={styles.titleScreen}>
                    Example
                </Text>
            </View>
        	    <View style={styles.containerOptional}>
                 <TouchableHighlight
                accessibilityLabel= {id}
                    underlayColor={'#f5f5f5f5'}        
                    style={styles.btnNav}
                    onPress={() => {
                        alert('HELLO');
                        fun()
                    }}
                >
                    <Image style={styles.ico} source={require('../../../assets/cart.png')} />
                </TouchableHighlight>
            </View>
        </View>
    )
}

const styles = StyleSheet.create({
    header : {
        backgroundColor : 'pink',
        height : 100,
        paddingLeft : 40,
        paddingRight : 50,
        flexDirection : 'row'

    },
    containerBack : {
        width : '70%',
        flexDirection : 'row',
        justifyContent : 'flex-start',
        alignItems : 'center',
        gap: 15,
    },
    containerOptional : {
        width : '30%',
        flexDirection : 'row',
        justifyContent : 'flex-end',
        alignItems : 'center',
    },
    titleScreen : {
        fontSize : 20,
    },
    btnNav : {
        width : 40,
        height : 40,
        borderRadius : 11,
        backgroundColor : 'white',
        justifyContent : 'center',
        alignItems : 'center',
        elevation : 8
    }
})


export default HeaderScreenModal
